# Implementation summary

Pinned `simai/ui v5.3.1` with `simai/ui-smart v5.3.0`, added registry and
`Smart::render()`, converted Atlas Button to `sf-button` and the shared
Dataview renderer to `sf-table`, and fixed JSON-schema runtime compatibility.
