# Independent review

Reverse outcome check confirmed real custom-element inner markup rather than
Larena CSS imitation. Runtime paths contain the pinned pair id and the DOM has
one boot, one core and one bridge script.
