# Review

The change is limited to the asset ownership boundary. It does not relax checks for components that declare CSS, modules, or Framework runtime resources. A renderer cannot add an undeclared asset because manifest and renderer keys are compared first.
