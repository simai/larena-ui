# Tests

- `SmartManagerTest`: native asset-free component renders with no activation and no tags.
- `FrontendRenderArtifactTest`: invalid empty graph without provenance remains rejected.
- Full UI package quality gate is the acceptance command for this packet.
