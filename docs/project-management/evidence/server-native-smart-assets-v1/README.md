# Server-native Smart asset contract

This packet records the UI contract correction required by the Larena login Composition Recipe. A native Smart component may declare no frontend assets and remain server-renderable without a Framework runtime activation. Components that declare assets retain the existing activation and immutable runtime-pair checks.
