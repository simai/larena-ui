# Implementation summary

- Pinned the published pair ui-2b9aa9635ad0-smart-db547bb87b6b and its immutable runtime artifact.
- Bound larena.registered-list to the published sf-table ports; the list host carries the scope endpoint and its title.
- The Dataview host answers context queries of a route target through the table sequence (latest result wins) and keeps the selection as a server filter.
- The admin event bridge asset revision follows the new bridge digest.
