# Tests

Planned command:

```bash
composer run quality:gate
```

Expected checks:

- package validator accepts the launch context;
- lint/analyse pass;
- `UiContractTest` proves carrier resource files exist and custom-element readiness can become complete;
- scope checker accepts only the allowlisted resource files and evidence path.

