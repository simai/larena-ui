# Smoke

Real browser smoke remains deferred to the root Larena admin runtime smoke batch.

This package batch only makes required carrier source files available under package-owned resources. A later batch must bind them through `larena/core:core.assets`, the admin shell/layout payload and the read-only API adapter, then verify console/network/write rejection in a browser.

