# Admin Smart Carrier Fallback Resources

This batch adds minimal Larena-owned fallback carrier modules for the admin smart resource pack browser-smoke path.

The modules register the required custom elements, but they do not create a frontend build pipeline, root runtime copy, route/controller runtime or public asset publication.

