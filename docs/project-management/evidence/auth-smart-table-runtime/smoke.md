# Smoke

The immutable artifact was built and verified from exact Git trees. Chrome
inspection of the Root users page exposed an adapter loss between Auth and the
Smart Table; the package regression now preserves the secondary email text.
Final Root browser acceptance remains application-scoped and is intentionally
not claimed here.
