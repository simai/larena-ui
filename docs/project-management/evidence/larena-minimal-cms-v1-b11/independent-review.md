# Focused review

The UI package keeps mandatory dependencies limited to Core and Dataview. Layout remains a development-only compatibility dependency and no Layout class is imported by the accepted Smart rendering path.
