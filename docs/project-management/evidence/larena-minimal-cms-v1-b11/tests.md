# Tests

- `composer run quality:gate`
- `composer run test:minimal-cms-ui`
- `composer validate --strict --no-check-publish`

The focused tests cover unknown component, renderer, prop and asset rejection plus escaped safe rendering.
