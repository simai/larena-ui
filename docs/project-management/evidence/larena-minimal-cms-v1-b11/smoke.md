# Smoke

An allowlisted Smart Component resolves its registered renderer, validates props, activates the pinned asset pair and returns escaped output. Unknown registry and contract inputs are rejected.
